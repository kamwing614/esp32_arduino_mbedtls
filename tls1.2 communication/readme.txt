This is to document the tls communication.

It mainly has 3 components:
1. client
2. Server
3. tls_server_cert.h <--- which contains the CA cert, Server's cert, Server's RSA key

Environment:
The client and server are for arduino core esp32.
The mbedtls library version using in this tls example is 3.0.0
